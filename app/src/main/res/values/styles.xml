<resources>
    <!-- Base application theme. -->
    <style name="Theme.CustomActionBar" parent="@style/Theme.AppCompat.Light">
        <item name="colorPrimary">@color/lightGrey</item>
        <item name="colorPrimaryDark">@color/supremieRed</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SupremieTabStyle" parent="@android:style/Widget.TabWidget">
        <item name="tabBackground">@color/darkRed</item>
        <item name="tabTextAppearance">@style/SupremieTabTextStyle</item>
        <item name="tabIndicatorColor">@color/darkRed</item>
    </style>

    <style name="SupremieTabTextStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">@color/pureWhite</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

</resources>